# CLAUDE.md - AgentCV

## Project

AgentCV.ai — "LinkedIn for AI Agents"
Next.js 15 + TypeScript + Tailwind CSS v4

## Design Philosophy

- 100% English UI — global/US market only
- Linear/Vercel aesthetic — dark mode, minimal, premium
- NO Korean text anywhere in the UI

## Quality Rules (MANDATORY)

- `npm run build` must pass with zero errors
- `npx tsc --noEmit` must pass (strict mode)
- All UI changes must be described in commit message
- Pre-commit hooks will auto-format on commit
